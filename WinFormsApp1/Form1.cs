namespace WinFormsApp1
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }


        private void BtnSaveCliente_Click(object sender, EventArgs e)
        {
            //Step 1: Receive data into a client model object
            //Clientes obj = new Clientes();
            // obj.id = int.Parse(txtCodCliente.Text); // assuming ID is an integer
            // obj.nome = txtNomeCliente.Text;
            // obj.telemovel = txtTelmCliente.Text;

            // // Step 2: Create an object of the ClienteDAO class and call the CadastrarCliente method
            // ClienteDAO dao = new ClienteDAO();
            //dao.CadastrarCliente(obj);
        }

        private void textBox1_TextChanged(object sender, EventArgs e)
        {

        }

        private void button2_Click(object sender, EventArgs e)
        {

        }
    }
}
